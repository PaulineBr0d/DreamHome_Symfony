{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
<h2>{{ title }}</h2>

{{ form_start(form) }}
    {{ form_row(form.name) }}
    <button>Ajouter</button>
{{ form_end(form) }}

<a href="{{ path('admin_types', {typeName: typeName}) }}">Retour à la liste</a>
{% endblock %}